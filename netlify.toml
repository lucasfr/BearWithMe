[build]
  command = "npx expo export --platform web && cp -r public/. dist/"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
